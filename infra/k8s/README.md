# Kubernetes

This directory will hold deployment manifests after the Docker Compose MVP is working.

## Planned Manifests
- Gateway deployment and service
- Agent deployment and service
- Kafka connectivity configuration
- KEDA `ScaledObject` definitions
