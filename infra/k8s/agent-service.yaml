apiVersion: v1
kind: Service
metadata:
  name: guardian-stream-agent
  namespace: guardian-stream
  labels:
    app.kubernetes.io/name: guardian-stream-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: guardian-stream-agent
  ports:
    - name: http
      port: 80
      targetPort: http
