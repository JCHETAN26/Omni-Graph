# Template only — do NOT commit real secrets.
# Replace with `kubectl create secret generic ... --from-literal=ANTHROPIC_API_KEY=...`
# or wire to External Secrets Operator / Vault.
apiVersion: v1
kind: Secret
metadata:
  name: guardian-stream-agent-secrets
  namespace: guardian-stream
  labels:
    app.kubernetes.io/name: guardian-stream-agent
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
