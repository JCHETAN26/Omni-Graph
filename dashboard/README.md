# Dashboard

The `dashboard/` app will provide observability for Guardian-Stream.

## Planned Views
- Request throughput and latency
- Blocked or redacted request counts
- Consumer lag and processing health
- Reasoning trace inspection

The dashboard is intentionally left unimplemented for Phase 1 so we can focus on the gateway-agent loop first.
