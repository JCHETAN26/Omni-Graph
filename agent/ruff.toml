line-length = 120
target-version = "py311"

[lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]  # line length is enforced by formatter

[lint.per-file-ignores]
# Scripts adjust sys.path before importing the agent package.
"scripts/*.py" = ["E402"]
